Fixed the Suricata bind-mount chown trap: `aptl lab start` no longer rewrites host-side ownership of `config/suricata/suricata.yaml`, `config/suricata/rules/local.rules`, or the `.aptl/suricata/rules/misp/` tree. Suricata config and MISP rules are now seeded into Compose named volumes and staged into the container by a wrapper entrypoint (ADR-043), so the upstream image's startup chown only ever touches Docker-managed paths. Repeated lab cycles leave the working tree clean and `pre-commit` passing.
