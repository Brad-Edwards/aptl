ACES realization now validates that the selected Compose profile set forms a valid project. If an activated service depends on a service the profile selection excludes (for example, the enterprise tier without the Wazuh core its workstation reports to), `interpret_provisioning_plan` raises an `aptl.provisioner.compose-project-invalid` diagnostic at realization time instead of failing later with a raw `docker compose` "undefined service" error.
