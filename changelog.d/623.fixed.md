**Detected stale Wazuh Indexer credentials during `aptl lab start`.** When the indexer's listener is up but rejects the configured `.env` credentials (HTTP 401), startup now emits a targeted diagnostic pointing at `aptl lab stop -v` recovery instead of a generic timeout message. The indexer readiness probe also no longer passes the password on the curl command line.
