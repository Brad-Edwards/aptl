Added the SCN-010 Wazuh indexer ACES steady-state inventory bundle, evidence
capture tooling, SDL encoding, parity inventory pointers, and validation
coverage. The SDL encodes the upstream OpenSearch 2.19.1 indexer image
provenance, Amazon Linux runtime, container/network/health/process/
environment/local-identity state, filesystem inventory, package and
vulnerability inventories, the typed `runtime.datastore_services` spine
(engine=opensearch, data_model=search_index) covering cluster identity,
single-node membership/roles, per-index partition shard geometry, persistence
paths, mutual-TLS transport security, OpenSearch settings, and plugin/template
names, plus the OpenSearch Security `runtime.identity_authorities` authority
carrying the six built-in internal users (bcrypt hashes redacted), curated
roles, role mappings, and the realized authc/authz chain. Three catalogued
surfaces that ACES cannot express today — per-index and cluster
cardinality/size/identity, structured index field mappings and template
bodies, and datastore node engine provenance — are captured as evidence and
recorded as blocked facts linked to filed ACES expressivity issues
(Brad-Edwards/aces#468, #469, #470) rather than forced into SDL prose; the
inventory issue stays open until those ACES surfaces land.
