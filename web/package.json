{
  "name": "aptl-web",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite dev",
    "build": "vite build",
    "preview": "vite preview",
    "check": "svelte-kit sync && svelte-check --tsconfig ./tsconfig.json",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@sveltejs/adapter-static": "^3.0.0",
    "@sveltejs/kit": "^2.70.1",
    "@sveltejs/vite-plugin-svelte": "^6.2.4",
    "@tailwindcss/vite": "^4.0.0",
    "@testing-library/svelte": "^5.2.0",
    "@types/dompurify": "^3.0.5",
    "@vitest/coverage-v8": "^4.1.10",
    "@xterm/addon-fit": "^0.11.0",
    "@xterm/addon-web-links": "^0.12.0",
    "@xterm/xterm": "^6.0.0",
    "jsdom": "^25.0.0",
    "svelte": "^5.56.8",
    "svelte-check": "^4.0.0",
    "tailwindcss": "^4.0.0",
    "typescript": "^5.7.0",
    "vite": "^7.3.6",
    "vitest": "^4.1.10"
  },
  "dependencies": {
    "dompurify": "^3.3.3",
    "marked": "^17.0.5"
  },
  "//overrides": "@sveltejs/kit 2.70.1 is the newest published release and still pins cookie@0.6.0, which carries GHSA-pxg6-pf52-xh8x (out-of-bounds characters accepted in cookie name/path/domain). There is no upstream release to upgrade to, so force the patched cookie release here. Drop this override once a kit release depends on cookie >=0.7.0.",
  "overrides": {
    "cookie": "^0.7.2"
  }
}
