**The ephemeral-lifecycle activity signal now considers only evidence files, not directory mtimes.**
`_latest_activity_at` walked the active run directory and counted directory
entries alongside files, so run-directory creation registered as fresh capture
activity and the recency signal tracked structural changes rather than when the
MCP servers last wrote evidence. It now restricts the scan to regular files,
matching the documented intent and making the recency comparison deterministic.
