SCN-010: captured the steady-state asset inventory for the `wazuh-sidecar-db` log-forwarding sidecar container and promoted it from a docker-compose-only `forwarding_agents` stub to the fully inventoried participant node `nodes.techvault.wazuh-sidecar-db` in `scenarios/techvault.sdl.yaml`. The SDL now encodes every catalogued participant/agent-observable runtime fact (local-build image identity and recipe, Debian 12 base, dpkg packages, Trivy vulnerabilities, filesystem and config manifest, local identity, processes, environment, the default capability set with no `CAP_NET_ADMIN`, security-net endpoint, and the no-network-listener posture), while the cross-node forwarding behaviour stays in the scenario-level `forwarding_agents` registry (Brad-Edwards/aces#460) — now enriched from the captured `ossec.conf` (tailed source, buffer policy) — and is resolved by the `db-logs-forwarded-wazuh` and new `wazuh-sidecar-db-forwards-wazuh-manager` relationship edges. The bundle ledger reports zero blocked facts; no ACES expressivity blocker remains.
