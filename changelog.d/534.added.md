Added four curated TechVault ACES SDL startup variants (`techvault-attacker-target`, `techvault-enterprise-web`, `techvault-defensive-min`, `techvault-observability-core`) selectable via `aptl lab start --scenario <id>`. Each is a small single-file scenario with a static realization proof that it selects a bounded Compose profile set from declared node content. The default `techvault-operational` startup behavior is unchanged.
