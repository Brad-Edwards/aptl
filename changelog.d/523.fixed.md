### Fixed

- Restored the TechVault scenario `conditions` block (per-node readiness conditions and the scenario-level healthcheck definitions) that the SDL module decomposition dropped, so the modular `scenarios/techvault.sdl.yaml` again validates and the inventory/audit tests pass. Also cited `scenarios/techvault-operational.sdl.yaml` in the ACES parity inventory.
