SCN-010: captured the steady-state asset inventory for the `suricata` IDS sensor container and encoded every cleanly-expressible runtime fact in `scenarios/techvault.sdl.yaml` (upstream image provenance, OS, network attachment, capabilities, mounts, the unix command socket, filesystem/ruleset inventory, local identity, packages, Trivy vulnerabilities, Suricata software identity, and the eve.json log-forwarding relationship). As the first passive network-sensor archetype in TechVault, two catalogued observables — the sensor's monitoring posture and its typed IDS/NDR detection-engine semantics — are blocked on ACES expressivity gaps (Brad-Edwards/aces#429, #430) and recorded as `blocked_by_aces_gap` facts; issue #345 stays open until those land.
