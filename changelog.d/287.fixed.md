### Fixed

- `aptl lab start` now seeds MISP Suricata rule baselines into `.aptl/suricata/rules/misp/` and mounts that generated directory for Suricata and `misp-suricata-sync`, so the sync service no longer rewrites checked-in `config/suricata/rules/misp/` files during lab startup.
