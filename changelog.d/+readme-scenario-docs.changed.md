**Surfaced selectable ACES SDL scenarios in the README.** The top-level `README.md` now documents that scenarios are ACES SDL documents selectable at startup via `aptl lab scenarios` and `aptl lab start --scenario <id>` / `--scenario-path <file>`, that Compose profiles are realized from the nodes a scenario declares rather than a fixed preset, and a new "Scenarios" section catalogs the operational default plus the four curated TechVault variants. The documentation index now links the [SDL Reference](docs/sdl/index.md) and [Curated TechVault Variants](docs/sdl/techvault-curated-variants.md) pages.
