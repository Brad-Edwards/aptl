The paper scenario (`scenarios/paper-agent-loop.sdl.yaml`) no longer models its evidence surfaces as content placements. The participant terminal observation is owned by its observation boundary and the runtime observation envelope; Wazuh corroboration and negative boundary checks are authored ACES `evidence_requirements`; and the participant runtime binding rides the behavior specification's `x-aptl:participant-runtime-binding` governed extension instead of being planted on Kali. Kali is registered as a content-capable service so the remaining participant-visible task brief lowers to a typed realization on the existing `kali_operations` volume, so the whole content surface realizes with no content hard-fail. The participant observation boundary now also keeps the internal DB and Wazuh endpoint identities out of its participant-visible projection.
