Added the SCN-010 completion-grade misp-redis ACES asset inventory: a
non-destructive steady-state evidence bundle, mapping ledger, and full SDL
encoding of the Redis backing-store runtime, including the first TechVault use of
`runtime.datastore_services` (key-value engine, persistence posture, logical-DB
partitions) and `runtime.app_authorizations` (the Redis ACL), plus the typed
MISP-to-Redis relationship and validation coverage.
