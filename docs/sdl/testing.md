# Historical SDL Testing Note

The APTL-local SDL parser, validator, compiler, and planner test suites were
removed after the ADR-035 RAES cutover. This page is retained only for older
links.

Current scenario verification lives in RAES parser/runtime tests plus APTL's
static and live TechVault validation gates.
