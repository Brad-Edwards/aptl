The APTL ACES backend now realizes the declared node health expectation (`runtime.health.status`) from the compiled ACES plan and enforces it during live conformance validation: a node that declares `healthy` whose container is not actually healthy is now a hard readiness failure instead of being silently tolerated.
