**Wazuh Indexer credentials no longer reach `curl` via the command line.** The startup readiness probe and the alert collector now send HTTP Basic auth through a 0600 header file (`Authorization: Basic …`) instead of `-u user:pass`, so the indexer password is no longer observable via `ps` or `/proc/<pid>/cmdline` (ADR-029).
