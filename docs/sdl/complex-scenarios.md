# Historical Complex Scenario Designs

The complex scenario designs formerly tracked here belonged to the APTL-local
SDL workstream. They are no longer current authoring guidance.

Current scenario work should use ACES SDL, the curated catalog, and the
TechVault ACES gate documents. Historical YAML fixtures that informed the old
designs are archived under `scenarios/archive/` as reference-only material.
