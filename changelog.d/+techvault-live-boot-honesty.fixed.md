`aptl lab start` no longer crashes when an ACES content placement carries free-form prose text (the participant-binding detector now treats unparseable content text as "not a binding" instead of raising), and the TechVault AD provisioner disables the domain password-complexity policy before creating users so deliberately-weak-password personas (e.g. `jessica.williams`) are actually realized instead of silently failing at `samba-tool user create`.
