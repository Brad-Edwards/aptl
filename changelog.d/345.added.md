SCN-010: captured the steady-state asset inventory for the `suricata` IDS sensor container and encoded every catalogued participant-observable runtime fact in `scenarios/techvault.sdl.yaml` (upstream image provenance, OS, network attachment, capabilities, mounts, the unix command socket, filesystem/ruleset inventory, local identity, packages, Trivy vulnerabilities, and Suricata software identity). As the first passive network-sensor archetype in TechVault, the sensor's monitoring posture and its typed IDS/NDR detection-engine semantics drove two ACES expressivity gaps (Brad-Edwards/aces#429, #430); both landed in ACES dev (`runtime.network_sensors`, `runtime.network_detection_engines`) and are now encoded, so no ACES expressivity blocker remains and the bundle ledger reports zero blocked facts.
