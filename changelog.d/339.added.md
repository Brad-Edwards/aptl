**Captured the TechVault kali container ACES inventory.** Added the kali
red-team container steady-state evidence bundle, mapping ledger, and README
under `docs/aces/inventory/kali/`, the TechVault ACES SDL `kali` node (plus the
`redteam-net` network), parity inventory rows, and structural tests for
SCN-010 issue #339. The capture rebuilt the `kali` image from current
`containers/kali/` source and recreated the container before capture. The SDL
encodes the kali node's image identity and build provenance, network
realization, SSH service, runtime mounts, filesystem inventory, process set,
environment, container-wide capability policy with the sshd subtree's
capsh capability override, restart/resource policy, container host/security
config including Docker `init`, `seccomp` posture, and `extra_hosts`, full
dpkg package inventory, Grype scanner findings, local identity database, and
the OBS-003 `ForceCommand`/`AcceptEnv` sshd policy. The four ACES expressivity
surfaces this inventory motivated — `Brad-Edwards/aces` #384 (ADR-027
init_process), #385 (ADR-028 seccomp_profile/security_opt), #386 (ADR-030
RuntimeCapabilityPolicy.process_overrides), and #387 (ADR-031
RuntimeConfiguration.ssh_servers) — landed on aces `dev` and are consumed in
this pass; no ACES expressivity blocker remains. Grype is the primary
vulnerability scanner because Trivy has no advisory database for the
`kali-rolling` release.
