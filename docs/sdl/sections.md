# Historical Section Reference

This page used to document the APTL-local SDL section model. That model is no
longer a supported authoring or runtime surface after the ADR-035 ACES cutover.

Use ACES SDL and the TechVault ACES validation gate documentation for current
scenario sections, imports, composition, and runtime handoff behavior.
