Documentation overhaul: Vale prose-lint gate (pre-commit + CI, ADR-038), mkdocs nav reconciled to the docs tree with `mkdocs build --strict` enforced in CI, docs published to GitHub Pages on main, and an accuracy sweep fixing stale access instructions (victim/kali host SSH ports, hardcoded credentials, removed deploy workflow, MCP server inventory).
