`aptl lab start` now reconciles the hash-pinned indexer/dashboard fixtures (`INDEXER_USERNAME`/`INDEXER_PASSWORD`, `DASHBOARD_USERNAME`/`DASHBOARD_PASSWORD`) to the values the indexer's checked-in `internal_users.yml` demo users accept, overriding a divergent user-supplied value with a clear warning instead of preserving it. Previously a `.env` value that a user chose for those keys (as `.env.example` invited) could not authenticate against the indexer, so start failed the authenticated-readiness gate and surfaced a misleading ~240-diagnostic SEM-218 `backend-contract-invalid` cascade; `.env.example` now documents those keys as auto-managed fixtures.
